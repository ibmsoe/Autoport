#!/bin/bash

if [ -z "$JAVA_TYPE" -a -z "$JAVA_VERSION" ]; then
  JAVA_TYPE=<%= @type %>
  JAVA_VERSION=<%= @version %>
fi

case "$JAVA_TYPE" in
  "openjdk")
    source /opt/openjdk-$JAVA_VERSION.sh
    ;;
  "ibm-java-sdk")
    source <%= @install_dir %>/ibm-java-$JAVA_VERSION.sh
    ;;
esac
